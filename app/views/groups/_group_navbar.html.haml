- page ||= false
.row
	.small-12.column.panel.callout
		%h1 Long Group Name Shown Here
	.small-10.column
		%dl.sub-nav
			%dd{:class => (page == :home ? "active" : "")}
				%a{:href => "#"}
					Home
			%dd{:class => (page == :members ? "active" : "")}
				%a{:href => "#"}
					Members
			%dd{:class => (page == :photos ? "active" : "")}
				%a{:href => "#"}
					Photos
			%dd{:class => (page == :discussions ? "active" : "")}
				%a{:href => "#"}
					Discussions
			%dd
				%a{:href => "#", :data => {:dropdown => "group-nav-drop"}}
					More
				%ul#group-nav-drop.f-dropdown{:data => {"dropdown-content" => true}}
					%li
						%a{:href => "#"}
							Option 1
					%li
						%a{:href => "#"}
							Option 2
	.small-2.column
		/ if member
		My Membership
		/ else
		/ Join Group
%br